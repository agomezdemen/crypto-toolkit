// Alejandro Gomez De Mendieta
// Educational cryptography CLI toolkit

use clap::Parser;

#[derive(Parser, Debug)]
#[command(
    name = "crypt",
    version,
    about = "Educational cryptography toolkit built with Rust"
)]
struct Args {}

fn main() {}
